#include<bits/stdc++.h>

using namespace std;
// ý tưởng: Phép duyệt inorder của cây nhị phân tìm kiếm là mảng tăng dần
int main(){
	int tc; cin >> tc;
	while(tc--){
		int n; cin >> n;
		set<int> se;
		for(int i=0; i<n; i++){
			int u, v; char c; cin >> u >> v >> c;
			se.insert(u);
			se.insert(v);
		}
		for(int x : se) cout << x << " ";
		cout << endl;
	}
	return 0;
}
