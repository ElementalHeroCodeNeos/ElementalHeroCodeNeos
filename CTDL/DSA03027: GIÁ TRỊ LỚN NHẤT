#include <bits/stdc++.h>

using namespace std;

using ll = long long;

int main(){
	int n; cin >> n;
	ll a[n];
	ll res = 0;
	for(int i=0; i<n; i++){
		cin >> a[i];
		if(a[i] > 0){
			res += 2ll * a[i];
		}
	}
	cout << res << endl;
	return 0;
}
	
