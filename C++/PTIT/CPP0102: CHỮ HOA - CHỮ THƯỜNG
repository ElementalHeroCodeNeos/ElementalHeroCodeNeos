#include <bits/stdc++.h>

using namespace std;

int main(){
	int tc; cin >> tc;
	while(tc--){
		char c; cin >> c;
		if(c >= 'A' && c <= 'Z'){
			c = tolower(c);
		}
		else{
			c = toupper(c);
		}
		cout << c << endl;
	}
	return 0;
}
