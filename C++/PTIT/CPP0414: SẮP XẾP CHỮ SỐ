#include <bits/stdc++.h>

using namespace std;

using ll = long long;

int main(){
	int tc; cin >> tc;
	while(tc--){
		int n; cin >> n;
		ll a[n];
		string s = "";
		set<char> se;
		for(int i=0; i<n; i++){
			cin >> a[i];
			s += to_string(a[i]);
		}
		for(char x : s){
			se.insert(x);
		}
		for(char x : se){
			cout << x << " "; 
		}
		cout << endl;
	}
	return 0;
}
