#include <bits/stdc++.h>

using namespace std;

int main(){
	int tc; cin >> tc;
	while(tc--){
		string s; cin >> s;
		string tmp = s;
		reverse(s.begin(), s.end());
		if(tmp == s) cout << "YES\n";
		else cout << "NO\n";
	}
	return 0;
}
