#include <bits/stdc++.h>

using namespace std;

int main(){
	int tc; cin >> tc;
	while(tc--){
		string s; cin >> s;
		string rev = s;
		reverse(rev.begin(), rev.end());
		if(rev == s) cout << "YES\n";
		else cout << "NO\n";
	}
	return 0;
}
