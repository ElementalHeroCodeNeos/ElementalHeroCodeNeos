#include <bits/stdc++.h>

using namespace std; 

int main(){
	ifstream in;
	in.open("DATA.in");
	int x;
	map<int, int> mp;
	while(in >> x){
		mp[x]++;
	}
	for(auto it : mp){
		cout << it.first << " " << it.second << endl;
	}
	in.close();
}
