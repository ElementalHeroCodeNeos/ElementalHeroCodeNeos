#include <bits/stdc++.h>

using namespace std;

int check(int n){
	while(n){
		int d = n%10;
		if(d!=0 && d!=6 && d!=8) return 0;
		n /= 10;
	}
	return 1;
}

int main(){
	int tc; cin >> tc;
	while(tc--){
		int n; cin >> n;
		if(check(n)) cout << "YES\n";
		else cout << "NO\n";
	}
	return 0;
}
