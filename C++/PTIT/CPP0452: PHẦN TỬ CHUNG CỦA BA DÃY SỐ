#include <bits/stdc++.h>

using namespace std;

using ll = long long;

int main(){
	int tc; cin >> tc;
	while(tc--){
		int n, m, p; cin >> n >> m >> p;
		ll a[n], b[m], c[p];
		for(int i=0; i<n; i++) cin >> a[i];
		for(int i=0; i<m; i++) cin >> b[i];
		for(int i=0; i<p; i++) cin >> c[i];
		vector<ll> v;
		int i=0, j=0, k=0;
		while(i<n && j<m && k<p){    // Phần tử chung có thể trùng nhau. Ví dụ có thể in: 20 20 80
			if(a[i] == b[j] && b[j] == c[k]){
				v.push_back(a[i]);
				i++;
				j++;
				k++;
			}
			else if(a[i] < b[j]) i++;
			else if(b[j] < c[k]) j++;
			else k++;
		}
		if(v.size() == 0) cout << "-1\n";
		else{
			for(int i=0; i<v.size(); i++) cout << v[i] << " ";
			cout << endl;
		}
	}
	return 0;
}
