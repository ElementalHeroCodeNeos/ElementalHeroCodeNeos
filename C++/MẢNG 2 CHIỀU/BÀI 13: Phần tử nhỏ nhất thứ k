#include <bits/stdc++.h>

using namespace std;

int main(){
	int tc; cin >> tc;
	while(tc--){
		int n, k; cin >> n >> k;
		int a[n*n];
		for(int i=0; i<n*n; i++){
			cin >> a[i];
		}
		sort(a, a + n*n);
		cout << a[k-1] << endl;
	}
	return 0;
}
