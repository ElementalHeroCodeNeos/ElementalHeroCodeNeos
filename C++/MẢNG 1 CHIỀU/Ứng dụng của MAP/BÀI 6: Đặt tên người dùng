#include <bits/stdc++.h>

using namespace std;

int main(){
	int n; cin >> n;
	cin.ignore();
	map<string, int> mp;
	while(n--){
		string s;
		getline(cin, s);
		if(mp.count(s) == 0){
			cout << s << endl;
		}
		else cout << s << mp[s] << endl;
		mp[s]++;
	}		
	return 0;
}
	
