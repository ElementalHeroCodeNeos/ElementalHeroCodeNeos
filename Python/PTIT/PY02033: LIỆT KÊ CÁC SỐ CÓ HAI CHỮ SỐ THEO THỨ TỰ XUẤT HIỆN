import math

if __name__ == '__main__':  
    s = input()
    se = set()
    a = []
    for i in range(0, len(s) - 1, 2):
        num = s[i] + s[i+1]
        if num not in se:
            a.append(num)
            se.add(num)
    print(' '.join(a))
