import math
import collections

if __name__ == '__main__': 
    tc = int(input())
    for case in range(tc):
        n = int(input())
        a = list(map(int, input().split()))
        d = collections.Counter(a)
        for x, y in d.items():
            if y % 2 == 1:
                print(x)
                break
