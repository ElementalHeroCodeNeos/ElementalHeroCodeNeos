import math

if __name__ == '__main__': 
    tc = int(input())
    for case in range(tc):
        n = int(input())
        a = list(map(int, input().split()))
        ans = a[0]
        for i in range(1, n):
            ans ^= a[i]    # Toán tử '^' (XOR)
        print(ans)
