
if __name__ == '__main__':   
    tc = int(input())
    for case in range(tc):
        s = input()
        chu = sorted([x for x in s if x.isalpha()])
        so = sum(int(x) for x in s if x.isdigit())
        print(''.join(chu) + str(so))
